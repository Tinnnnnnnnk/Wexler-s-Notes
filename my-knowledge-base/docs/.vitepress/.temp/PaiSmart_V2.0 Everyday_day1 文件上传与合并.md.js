import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"PaiSmart/V2.0 Everyday/day1 文件上传与合并.md","filePath":"PaiSmart/V2.0 Everyday/day1 文件上传与合并.md","lastUpdated":1772161575000}');
const _sfc_main = { name: "PaiSmart/V2.0 Everyday/day1 文件上传与合并.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><p>你这段描述<strong>已经很接近“能过关”的水平了</strong>✅（至少说明你不是背概念，而是真的顺着接口/代码走过一遍）。<br> 但如果放到面试场景，面试官往往会继续追两类东西：</p><ol><li><p><strong>“你说的每一步，有没有具体证据/关键细节？”</strong>（Redis key、MinIO objectKey、bucket、chunkSize、状态码）</p></li><li><p><strong>“异常与幂等你怎么兜？”</strong>（重复上传、标记失败、二次 merge、URL 过期、并发上传）</p></li></ol><p>下面我按“你说得对的 + 需要补的 + 面试版话术”给你一套可直接拿去讲的升级建议 🎯</p><hr><h2 id="你理解得不错的地方-面试官会认可的点-✅" tabindex="-1">你理解得不错的地方（面试官会认可的点）✅ <a class="header-anchor" href="#你理解得不错的地方-面试官会认可的点-✅" aria-label="Permalink to &quot;你理解得不错的地方（面试官会认可的点）✅&quot;">​</a></h2><p>你已经把核心链路讲全了：</p><ul><li><p><strong>先登录拿 token</strong> → 再调用 <code>/api/v1/upload/chunk</code>（分片上传）和 <code>/api/v1/upload/merge</code>（合并）</p></li><li><p><strong>第 0 块做文件类型校验</strong>（避免无效类型进入后续解析链路）</p></li><li><p>UploadService 里做了“三件事”：</p><ol><li><p><code>file_upload</code> 是否存在，不存在就创建记录</p></li><li><p>Redis Bitmap 判断该 chunk 是否已上传（断点续传/幂等）</p></li><li><p>分片写 MinIO → Redis setBit 标记 → MySQL 保存 chunk 记录</p></li></ol></li><li><p>合并接口里：</p><ul><li><p>判断分片是否齐全</p></li><li><p>MinIO 合并</p></li><li><p>Kafka 异步推进后续 Tika 解析</p></li></ul></li></ul><p>这套<strong>流程层面的叙述</strong>，对于实习面试来说，已经比大多数“只会说 RAG”强很多 👍</p><hr><h2 id="但你现在的回答-在面试里会被追问卡住的点-必须补-⚠️" tabindex="-1">但你现在的回答，在面试里会被追问卡住的点（必须补）⚠️ <a class="header-anchor" href="#但你现在的回答-在面试里会被追问卡住的点-必须补-⚠️" aria-label="Permalink to &quot;但你现在的回答，在面试里会被追问卡住的点（必须补）⚠️&quot;">​</a></h2><p>我直接用你上传的 <code>UploadController.java / UploadService.java</code> 的真实实现来对齐你说法（不靠猜）。</p><h3 id="_1-文件类型校验失败返回-403-——这里你说错了" tabindex="-1">1) “文件类型校验失败返回 403”——这里你说错了 <a class="header-anchor" href="#_1-文件类型校验失败返回-403-——这里你说错了" aria-label="Permalink to &quot;1) “文件类型校验失败返回 403”——这里你说错了&quot;">​</a></h3><p>在 <code>uploadChunk</code> 里校验失败返回的是 <strong>400 BAD_REQUEST</strong>，并且会返回 supportedTypes 等信息；<br> 真正的 <strong>403</strong> 出现在 <code>mergeFile</code> 里“权限不足”的分支。<br> ✅ 面试时你要说对：</p><ul><li><p><strong>类型不支持 → 400</strong></p></li><li><p><strong>越权操作 → 403</strong></p></li></ul><blockquote><p>这类细节面试官特别爱拿来判断：你是“看过代码”，还是“听别人讲过”。</p></blockquote><hr><h3 id="_2-你少讲了-3-个-关键标识符-讲出来会立刻显得你很懂" tabindex="-1">2) 你少讲了 3 个“关键标识符”，讲出来会立刻显得你很懂 <a class="header-anchor" href="#_2-你少讲了-3-个-关键标识符-讲出来会立刻显得你很懂" aria-label="Permalink to &quot;2) 你少讲了 3 个“关键标识符”，讲出来会立刻显得你很懂&quot;">​</a></h3><p>面试里讲流程不如讲“关键数据怎么流动”。这 3 个你一定要能脱口而出：</p><p><strong>① Redis Bitmap 的 key 长什么样？</strong><br> 在 UploadService 里是：<br><code>upload:{userId}:{fileMd5}</code><br> （你现在只说“根据 fileMd5”，少了 userId 这一层隔离）</p><p><strong>② MinIO 分片 objectKey 怎么命名？</strong><br> 分片路径是：<br><code>chunks/{fileMd5}/{chunkIndex}</code><br> bucket 写死是 <code>&quot;uploads&quot;</code></p><p><strong>③ 合并后的 objectKey 怎么命名？</strong><br> 合并路径是：<br><code>merged/{fileMd5}</code><br> 然后返回的是 <strong>预签名 URL（1 小时有效）</strong>。</p><p>这些细节说出来，面试官会立刻觉得：嗯，这人真的跑过、断点看过。</p><hr><h3 id="_3-totalchunks-参数-你传了-但服务端其实没用它-面试官会问" tabindex="-1">3) “totalChunks 参数”你传了，但服务端其实没用它（面试官会问） <a class="header-anchor" href="#_3-totalchunks-参数-你传了-但服务端其实没用它-面试官会问" aria-label="Permalink to &quot;3) “totalChunks 参数”你传了，但服务端其实没用它（面试官会问）&quot;">​</a></h3><p>你 Apifox 里传了 <code>totalChunks=1</code>，但 UploadService 的 <code>getTotalChunks()</code> <strong>不是读请求参数</strong>，而是：</p><ul><li><p>从 <code>file_upload.totalSize</code> 取文件总大小</p></li><li><p>固定 chunkSize = <strong>5MB</strong></p></li><li><p><code>ceil(totalSize / 5MB)</code> 计算 expectedChunks</p></li></ul><p>✅ 面试话术应该是：</p><blockquote><p>“服务端不信任客户端传的总分片数，而是根据 totalSize + 固定分片大小计算 expectedChunks，避免前端参数被篡改导致合并错判。”</p></blockquote><p>这句话很加分。</p><hr><h3 id="_4-你讲了-幂等-但没讲清-它到底怎么幂等" tabindex="-1">4) 你讲了“幂等”，但没讲清“它到底怎么幂等” <a class="header-anchor" href="#_4-你讲了-幂等-但没讲清-它到底怎么幂等" aria-label="Permalink to &quot;4) 你讲了“幂等”，但没讲清“它到底怎么幂等”&quot;">​</a></h3><p>UploadService 的分片幂等不是一句“用了 bitmap”就完了，它其实是<strong>三重校验</strong>，你把这个讲出来会很稳：</p><ul><li><p>Redis <code>getBit</code>：chunk 是否标记已上传</p></li><li><p>MySQL <code>chunk_info</code>：chunk 记录是否存在（如果 Redis 有但 DB 没有，会补写）</p></li><li><p>MinIO <code>statObject</code>：分片对象是否真的存在（Redis 标记了但 MinIO 没对象，会强制重新上传）</p></li></ul><p>✅ 面试话术一句话总结：</p><blockquote><p>“用 Redis bitmap 做快速幂等判断，同时用 DB/MinIO 做一致性兜底：Redis 可能丢，但 MinIO/DB 还能证明分片是否真实存在。”</p></blockquote><hr><h3 id="_5-合并后你漏了-收尾动作-面试官会继续问你-合并完怎么办" tabindex="-1">5) 合并后你漏了“收尾动作”，面试官会继续问你“合并完怎么办” <a class="header-anchor" href="#_5-合并后你漏了-收尾动作-面试官会继续问你-合并完怎么办" aria-label="Permalink to &quot;5) 合并后你漏了“收尾动作”，面试官会继续问你“合并完怎么办”&quot;">​</a></h3><p>合并逻辑里不仅 composeObject，还做了：</p><ul><li><p><code>removeObject</code> 删除每个 chunk（释放存储）</p></li><li><p><code>deleteFileMark</code> 删除 Redis bitmap key</p></li><li><p>更新 <code>file_upload.status=1</code> 并记录 <code>mergedAt</code></p></li><li><p>返回 <strong>预签名 URL（1 小时）</strong></p></li></ul><p>✅ 你面试时要把“清理 + 状态机”补上，不然面试官会追问“那分片留着不就爆桶了吗？”</p><hr><h2 id="你这段回答能不能直接在面试用-可以-但建议换成更-面试友好-的版本-✅" tabindex="-1">你这段回答能不能直接在面试用？可以，但建议换成更“面试友好”的版本 ✅ <a class="header-anchor" href="#你这段回答能不能直接在面试用-可以-但建议换成更-面试友好-的版本-✅" aria-label="Permalink to &quot;你这段回答能不能直接在面试用？可以，但建议换成更“面试友好”的版本 ✅&quot;">​</a></h2><p>你现在的回答像“流水账”，面试要讲成“流程 + 关键设计点”。<br> 我给你一段<strong>1 分钟版标准回答</strong>，你背这个就够扛住 80% 深挖：</p><hr><h2 id="✅-面试-1-分钟回答模板-建议直接背" tabindex="-1">✅ 面试 1 分钟回答模板（建议直接背） <a class="header-anchor" href="#✅-面试-1-分钟回答模板-建议直接背" aria-label="Permalink to &quot;✅ 面试 1 分钟回答模板（建议直接背）&quot;">​</a></h2><blockquote><p>我们的上传链路是分片上传 + 合并 + 异步处理三段式。<br> 用户登录拿到 token 后调用 <code>/api/v1/upload/chunk</code> 上传分片。第 0 片会做文件类型校验，不支持的类型直接 400 返回。服务端会先在 <code>file_upload</code> 表里按 <code>(fileMd5, userId)</code> 建立上传记录，保存 totalSize、orgTag、isPublic 等权限信息。<br> 分片幂等用 Redis bitmap：key 是 <code>upload:{userId}:{fileMd5}</code>，通过 <code>getBit/setBit</code> 判断某个 chunkIndex 是否已上传；MinIO 分片对象按 <code>chunks/{fileMd5}/{chunkIndex}</code> 写入 <code>uploads</code> bucket；写成功后 setBit，再把 chunk 的 md5/路径落到 <code>chunk_info</code> 表。<br> 合并时调用 <code>/api/v1/upload/merge</code>，先校验文件记录归属与权限，然后根据 totalSize 和固定分片大小计算 expectedChunks，并用 bitmap/DB 检查分片齐全。合并在 MinIO 侧用 <code>composeObject</code> 生成 <code>merged/{fileMd5}</code>，合并后清理分片对象、删除 bitmap 标记并更新 <code>file_upload</code> 状态。最后把包含 <code>fileMd5、objectUrl、fileName、userId、orgTag、isPublic</code> 的任务发送到 Kafka，消费者异步用 Tika 解析并进入后续向量化入库流程。</p></blockquote><hr><h2 id="面试官高频追问-你提前准备这-6-个点-基本稳-🔥" tabindex="-1">面试官高频追问（你提前准备这 6 个点，基本稳）🔥 <a class="header-anchor" href="#面试官高频追问-你提前准备这-6-个点-基本稳-🔥" aria-label="Permalink to &quot;面试官高频追问（你提前准备这 6 个点，基本稳）🔥&quot;">​</a></h2><h3 id="q1-为什么用-redis-bitmap" tabindex="-1">Q1：为什么用 Redis Bitmap？ <a class="header-anchor" href="#q1-为什么用-redis-bitmap" aria-label="Permalink to &quot;Q1：为什么用 Redis Bitmap？&quot;">​</a></h3><p>答：<strong>空间极省 + 原子位操作 + 快速判断缺失分片</strong>。比如 10 万分片也就 10 万 bit ≈ 12.5KB。</p><h3 id="q2-如果重复上传同一分片会怎样" tabindex="-1">Q2：如果重复上传同一分片会怎样？ <a class="header-anchor" href="#q2-如果重复上传同一分片会怎样" aria-label="Permalink to &quot;Q2：如果重复上传同一分片会怎样？&quot;">​</a></h3><p>答：先 <code>getBit</code>，如果已上传直接跳过；并且会检查 DB 和 MinIO 是否一致：Redis 标记但 MinIO 丢了会重传，Redis 标记但 DB 没记录会补写。</p><h3 id="q3-合并为什么用-minio-composeobject" tabindex="-1">Q3：合并为什么用 MinIO composeObject？ <a class="header-anchor" href="#q3-合并为什么用-minio-composeobject" aria-label="Permalink to &quot;Q3：合并为什么用 MinIO composeObject？&quot;">​</a></h3><p>答：<strong>合并发生在存储端</strong>，服务端不需要把所有分片拉回本地再拼，省网络、省内存，也避免 OOM。</p><h3 id="q4-totalchunks-谁说了算" tabindex="-1">Q4：totalChunks 谁说了算？ <a class="header-anchor" href="#q4-totalchunks-谁说了算" aria-label="Permalink to &quot;Q4：totalChunks 谁说了算？&quot;">​</a></h3><p>答：服务端根据 <code>file_upload.totalSize</code> + 固定 chunkSize 计算，不信任客户端参数。</p><h3 id="q5-合并后分片怎么处理" tabindex="-1">Q5：合并后分片怎么处理？ <a class="header-anchor" href="#q5-合并后分片怎么处理" aria-label="Permalink to &quot;Q5：合并后分片怎么处理？&quot;">​</a></h3><p>答：删除分片对象、删除 bitmap key、更新 file_upload 状态，避免存储泄漏。</p><h3 id="q6-kafka-在这里的价值是什么" tabindex="-1">Q6：Kafka 在这里的价值是什么？ <a class="header-anchor" href="#q6-kafka-在这里的价值是什么" aria-label="Permalink to &quot;Q6：Kafka 在这里的价值是什么？&quot;">​</a></h3><p>答：合并后立即返回，后续解析/向量化耗时任务异步处理，削峰填谷，也避免用户请求线程被长任务占用。</p><hr><h2 id="给你一个-更高级但非常真实-的改进建议-说出来会加分-✨" tabindex="-1">给你一个“更高级但非常真实”的改进建议（说出来会加分）✨ <a class="header-anchor" href="#给你一个-更高级但非常真实-的改进建议-说出来会加分-✨" aria-label="Permalink to &quot;给你一个“更高级但非常真实”的改进建议（说出来会加分）✨&quot;">​</a></h2><p>你现在实现里有两个很值得面试时提一句的工程思考（不要说成你已实现，说“我会这么改”）：</p><ol><li><p><strong>merge 目前返回的是 1 小时有效的预签名 URL，还把它传给 Kafka</strong></p><ul><li>如果 Kafka 堆积 &gt; 1h，消费者拿到 URL 可能过期<br> ✅ 改进：任务里传 <code>bucket + objectKey</code>（<code>uploads + merged/{fileMd5}</code>），消费者需要时自己生成预签名 URL 或直接用 MinIO SDK 拉取。</li></ul></li><li><p><strong>merge 的“是否齐全”先看 Redis，再 mergeChunks 内又看 DB/MinIO</strong></p><ul><li>Redis 标记失败可能导致误判“缺分片”<br> ✅ 改进：merge 前可用 <code>chunk_info</code> 数量作为兜底，或者只要 DB/MinIO 满足就允许 merge。</li></ul></li></ol><p>这种“我能指出边界并给出改法”的表达，比背八股强多了。</p><hr><h2 id="你下一步该怎么做-让-day1-证据更硬" tabindex="-1">你下一步该怎么做（让 Day1 证据更硬） <a class="header-anchor" href="#你下一步该怎么做-让-day1-证据更硬" aria-label="Permalink to &quot;你下一步该怎么做（让 Day1 证据更硬）&quot;">​</a></h2><p>你已经能说流程了，Day1 的升级就是把下面这 5 个值在断点里“抓出来”，当作面试证据：</p><ul><li><p>Redis key：<code>upload:{userId}:{fileMd5}</code></p></li><li><p>bucket：<code>uploads</code></p></li><li><p>chunk objectKey：<code>chunks/{fileMd5}/{chunkIndex}</code></p></li><li><p>merged objectKey：<code>merged/{fileMd5}</code></p></li><li><p>expectedChunks 的计算依据：<code>totalSize / 5MB</code></p></li></ul><p>把这 5 个拿到手，你的“我懂上传链路”就不再是口嗨，是证据链 ✅</p><hr><p>整体评价：你现在的理解 <strong>已经够用</strong>，但还差一步就能从“会讲流程”升级成“讲得像做过线上系统”。<br> 把我上面这几个细节补齐，你面试说这段，基本没人能把你问崩 😄🎸</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("PaiSmart/V2.0 Everyday/day1 文件上传与合并.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const day1________ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  day1________ as default
};
