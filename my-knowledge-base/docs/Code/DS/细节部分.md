- int转换成Long，使用Long.valueOf( value )
- 如果需要将一个List集合复制一份并使用，最好的办法就是使用构造函数——           List< Integer> dest=new ArrayList<>( src );
- 如果想要对一个集合中的所有数进行**数值操作**，可以使用list.replaceAll(x->x-root.val);
- 如果要把一个字符数组变成一个字符串，不可以使用 `toString`,需要使用`String string =  new String(charArray);`