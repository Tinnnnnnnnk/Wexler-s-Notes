---
tags:
  - LeetCode
  - 二叉树
  - 递归
difficulty: Medium
status: ✅ 已解决
date: 2026-02-26
---

# 114. 二叉树展开为链表 (Flatten Binary Tree to Linked List)

> [!info]- 题目描述 (点击展开)
> > 给你二叉树的根结点 `root` ，请你将它展开为一个单链表：
> > - 展开后的单链表应该同样使用 `TreeNode` ，其中 `right` 子指针指向链表中下一个结点，而左子指针始终为 `null` 。
> > - 展开后的单链表应该与二叉树 **先序遍历** 顺序相同。

---

## 💡 解题思路 (Kasumi's Memo)

### 核心策略 (信任飞跃 Leap of Faith)
1.  **第一步（定契约）：** 定义一个黑盒函数 `flattenAndReturnTail(root)`。承诺它能把以 `root` 为根的树拉平，并且向上级返回拉平后的**队尾节点**。
2.  **第二步（派任务）：** 无条件信任分身！调用黑盒函数处理左子树，拿到 `leftTail`；处理右子树，拿到 `rightTail`。
3.  **第三步（做拼接）：** 如果左边有队伍，把原来的右子树存起来（`temp = root.right`），把拉平的左子树接到根节点的右边，并清空左手（`root.left = null`）。最后把 `temp` 接到 `leftTail.right` 的后面。
4.  **第四步（作汇报）：** 向上级返回当前这棵树拉平后的总队尾（优先级：`rightTail` > `leftTail` > `root` 自己）。

### 为什么选择这个解法？
完全抛弃了“走一步看一步”的 `while` 循环寻路。通过自底向上的纯递归，让每个子树自己汇报自己的“队尾”，直接避免了断链和空指针异常，且时间复杂度最优。

---

## 💻 代码实现 (Java)

```java
class Solution {
    public void flatten(TreeNode root) {
        // 主函数只负责调用，不需要管返回值
        flattenAndReturnTail(root);
    }

    // 🌟 核心递归函数：拉平以 root 为根的树，并返回拉平后的“最后一个节点（队尾）”
    private TreeNode flattenAndReturnTail(TreeNode root) {
        // 1. 递归的尽头 (Base Case)
        if (root == null) {
            return null;
        }

        // 2. 派两个分身去拉平左右子树，并把它们各自的“队尾”带回来
        TreeNode leftTail = flattenAndReturnTail(root.left);
        TreeNode rightTail = flattenAndReturnTail(root.right);

        // 3. 关键的拼接环节：如果左边有队伍，才需要折腾
        if (leftTail != null) {
            TreeNode temp = root.right; // 暂存原生右子树
            
            root.right = root.left;     // 左子树换到右边
            root.left = null;           // 【极其重要】斩断左手！
            
            leftTail.right = temp;      // 原来的右子树接到左队伍的队尾
        }

        // 4. 向上级汇报总队尾
        if (rightTail != null) {
            return rightTail;
        } else if (leftTail != null) {
            return leftTail;
        } else {
            return root;
        }
    }
}
```

---

## 📊 复杂度分析
- **时间复杂度：** $O(N)$
  - *分析：* 每个节点恰好被访问和处理一次，没有多余的 `while` 循环去重复寻找队尾。
- **空间复杂度：** $O(H)$
  - *分析：* $H$ 为树的高度。空间消耗主要来自于递归调用的系统栈空间。最坏情况下（树退化为链表）为 $O(N)$，平衡树下为 $O(\log N)$。

---

## 📝 错题本 / 复盘
> [!failure] 遇到的坑 / 错误点
> > 1. **递归与迭代混用：** 试图在处理完左子树后，用 `while (root.right != null)` 去手动寻找队尾，导致逻辑极其复杂，一旦遇到嵌套极深的右子树，极易迷失指针导致空指针异常。
> > 2. **忘记清空左指针：** 在把左子树移到右边后，没有执行 `root.left = null`，导致树的结构混乱，出现环路或判题失败。
> > 3. **Base Case 拦截过早：** 错误地使用了 `if (root.left == null) return;`，导致只有右子树但极其庞大的节点被直接跳过，未能被彻底拉平。

> [!tip] 关键技巧 / 总结
> > **永远不要试图在脑子里单步调试递归！** 把递归函数当成一个“黑盒”，明确它“接收什么”、“干什么”、“返回什么”。在当前层只写基于黑盒返回结果的组装逻辑（老板思维）。

---

## 🧠 深度思考与感悟 (Reflections)
- **💡  触类旁通：** 这种“让左右子树各自汇报状态，根节点再做整合”的思维模式，是解决几乎所有二叉树 Hard 题（如二叉树的最大路径和、最近公共祖先）的标准起手式。
- **🔧 工程思维：** 函数的职责应该单一且明确。分身（递归）就负责拉平并找尾巴，不要让主流程去干寻路（`while` 循环）的脏活累活。这就是代码架构中的“职责分离”。
- **✨ 瞬间感悟：** 所谓“信任的飞跃（Leap of Faith）”，就是写递归时必须盲目自信：相信我的递归函数一定能完美处理好子问题，我只需要踩在它的肩膀上完成最后一块拼图即可。大脑内存有限，学会把计算“外包”给系统栈！